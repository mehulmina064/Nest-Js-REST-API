import { Repository, Entity } from 'typeorm';
// Create a decorator for the organization that filters queries by the user's organization

import { createParamDecorator, ExecutionContext } from "@nestjs/common";


